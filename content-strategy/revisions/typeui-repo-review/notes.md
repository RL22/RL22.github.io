## Verification markers

- None.

## Structural changes

The review now follows the required single-repository sequence: editorial hero, generic-UI problem, TypeUI workflow with linked GitHub preview, inspected artifacts, personal evaluation, use cases, and limits. It replaces the outdated implication of a shell-script artifact with the current `npx typeui.sh` CLI and TypeScript-backed registry workflow, while distinguishing agent instructions from runtime enforcement.

## Scope and metadata update

Added a note that TypeUI has expanded since the review was researched and now leads with MCP integration, plugins, and prompt surfaces, while keeping the review explicitly scoped to the CLI/Markdown workflow originally inspected. Softened the listing title and blurb so they describe TypeUI as design guidance in an agent's context without claiming that it forces compliance or prevents generic output.
